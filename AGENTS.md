# SentinelWatch - AGENTS.md

> Auto-generated by `C:\dev\_scripts\generate-agents-md.ps1` v1 on 2026-04-30.
> Hand-edit freely - this file won't be regenerated unless you delete it or run with `-Force`.

## Stack

- **Detected:** Python


## Run / build

```bash
pip install -r requirements.txt
```

## Repo location

`C:\dev\SentinelWatch`

## Stack-specific notes

**Patterns to honor:**
- Inspect `requirements.txt` / `pyproject.toml` for the entrypoint
- If Flask/FastAPI: usually `python app.py` or `uvicorn main:app --reload`
- If a CLI tool: `python -m <module>`

---

## Cross-AI Atmosphere (READ FIRST)

Before starting work in this repo, **read** `C:\Users\nickg\claude-obsidian\wiki\_atmosphere\now.md` to see what other AIs are doing. Append your own status entry. See `_atmosphere/README.md` for the full convention.

If both AIs need this repo simultaneously, use git worktrees (`_atmosphere/worktrees.md`).

## Engineering principles (locked, from ~/AGENTS.md)

1. **Think before coding.** Surface tradeoffs. Ask when unclear.
2. **Simplicity first.** Minimum code, no speculative abstraction.
3. **Surgical changes.** Touch only what you must. Match existing style.
4. **Goal-driven execution.** Verify before claiming done.

## Where the global context lives

- `C:\Users\nickg\AGENTS.md` - global engineering principles + infra map + active projects
- `C:\Users\nickg\claude-obsidian\` - Obsidian wiki, the shared brain across projects
- `C:\Users\nickg\.claude\plans\` - approved implementation plans (read the most recent before structural changes)

## When in doubt

Ask the user. The Karpathy guidelines bias toward asking, not assuming.
